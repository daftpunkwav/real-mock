import type { SystemGrowthInsights } from "@/lib/api/clients";

/** System self-growth aggregate response (same origin as the structure returned by getSystemInsights). */
export type SystemInsights = SystemGrowthInsights;

/**
 * @file profileLimits
 * @description Re-export field max-length constants derived from profileFields.
 *
 * Responsibilities:
 * - Keep existing import paths for section components and hooks
 *
 * Limits themselves live in profileFields.ts.
 */

export {
  PROFILE_FIELD_LIMITS,
  TECH_DOMAIN_ITEM_MAX,
  TECH_DOMAINS_MAX_COUNT,
} from "./profileFields";

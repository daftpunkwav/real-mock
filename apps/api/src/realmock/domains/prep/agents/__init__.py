"""Prep agent package: turn orchestration (agent/chat), per-turn toolset policy
(turn_tools), mid-turn compaction (round_compaction), context assembly
(context), tool registry, streaming helpers, per-turn state (turn_state),
user-prompt control flow (ask_user), inline-quiz rendering (quiz_render),
and tool execution policy (tool_exec)."""

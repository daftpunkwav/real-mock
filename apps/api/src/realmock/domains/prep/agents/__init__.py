"""Prep agent package: turn orchestration (agent/chat), context assembly, tool
registry, streaming helpers, per-turn state (turn_state), user-prompt control
flow (ask_user), inline-quiz rendering (quiz_render), and tool execution
policy (tool_exec)."""

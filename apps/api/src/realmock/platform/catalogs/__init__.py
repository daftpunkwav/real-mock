"""Cross-service read-only directory (data + lookup only, not a capability).

Place reference data shared by the interview and prep domains here; do not put it in one service and import it from another.
"""
